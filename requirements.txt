# Core dependencies (excluding torch, torchvision, torchaudio since we install them via buildCommand)
scikit-learn
numpy
pillow

# FastAPI & server
fastapi
uvicorn[standard]
python-multipart
python-dotenv
